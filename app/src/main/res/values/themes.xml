<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GuiaPocket" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cores principais -->
        <item name="colorPrimary">@color/primaryRed</item>
        <item name="colorPrimaryVariant">@color/primaryRedDark</item>
        <item name="colorOnPrimary">@color/textPrimary</item>

        <item name="colorSecondary">@color/accentGold</item>
        <item name="colorOnSecondary">@color/textPrimary</item>

        <!-- Fundo -->
        <item name="android:colorBackground">@color/background</item>

        <!-- Texto -->
        <item name="android:textColor">@color/textPrimary</item>

        <!-- Superfície e divisores -->
        <item name="colorSurface">@color/surface</item>
    </style>


</resources>